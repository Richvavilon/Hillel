username = input('Please enter your name ')
greetings = f'Hello, {username} nice to meet you!'
print(greetings)

some_num = float(input('Enter number '))
print(some_num, int(some_num), int(some_num) ** 4, int(some_num) * 0.5, int(some_num) % 2, sep='\n')
